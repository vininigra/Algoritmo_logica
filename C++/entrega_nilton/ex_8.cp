#include <iostream>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */
using namespace std;
int main() {
	 
	int vet[a];
	int i;
	
	
	cout<<"Escreva dois numeros para mostrar na matriz"<<endl;
	cin>>vet[a];
	for(i=0,i<=a, i++){
		
	}
	
//	cin>>vet[b];
//	cout<<vet[a]<<"|"<<vet[b];
	return 0;
}
